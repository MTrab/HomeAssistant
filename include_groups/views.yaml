default_view:
  view: yes
  icon: mdi:lightbulb
  entities:
    - group.livingroom
    - group.diningroom
    - group.kitchen
    - group.bedroom
    - group.hall
    - group.utility
    - group.christian
    - group.stairs
    - group.outside
    - device_tracker.morten_oneplus
    - device_tracker.britta_mobil
    - device_tracker.christian_motorola
    - device_tracker.michael_huawei

heating_view:
  view: yes
  icon: mdi:thermostat
  entities:
    - group.radiators
    - group.heat_control_group
    - group.heat_control
    - group.heat_control_weekdays
    - group.heat_control_weekends

cover_view:
  view: yes
  icon: mdi:blinds
  entities:
    - group.covers_diningroom
    - group.covers_livingroom

temperature_view:
  view: yes
  icon: mdi:thermometer
  entities:
    - group.temperature_1stfloor

doorbell_view:
  view: yes
  icon: mdi:bell
  entities:
    - group.doorbell_sound_devices
    - group.doorbell_control

landroid_view:
  view: yes
  icon: mdi:robot-vacuum-variant
  entities:
    - sensor.landroid_battery
    - sensor.landroid_wifi_quality
    - sensor.landroid_total_time
    - sensor.landroid_total_distance
    - group.landroid

kamstrup_view:
  view: yes
  icon: mdi:flash
  entities:
    - group.kamstrup

#weather_view:
#  view: yes
#  icon: mdi:weather-cloudy
#  entities:
#    - group.weather

battery_view:
  view: yes
  icon: mdi:battery
  entities:
    - group.thermostat_battery
    - group.mysensors_battery
    - group.hue_battery

test_view:
  view: yes
  icon: mdi:test-tube
  entities:
    - group.test
    - group.hue_motion_test
    - group.hue_motion_test_hall
    - group.zw100_test
    - group.lkpanel_test
    - group.effect_police

camera_view:
  view: yes
  icon: mdi:camera
  entities:
    - camera.carport
    - camera.driveway
    - camera.frontdoor
    - camera.garden_west
    - camera.hall
